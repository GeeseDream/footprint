================================
Git 使用
================================

只获取仓库指定分支的方法

.. code-block:: shell

    git clone --branch 分支名称 仓库_url
    git clone -b tags 仓库_url
    git clone --single--branch 分支名称 仓库_url